name: Validate Universo SO 0610 · legacy archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Validator 0610 archivado: la superficie vigente de Universo SO es 0.83.x."
