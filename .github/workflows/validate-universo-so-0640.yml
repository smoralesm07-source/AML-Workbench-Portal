name: Validate Universo SO 0640 · legacy archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Validator 0640 archivado: la superficie vigente de Universo SO es 0.83.x."
