name: Deploy ATLAS AML v2

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: atlas-pages-v2
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1

      - name: Validate 2.0.2 entity intelligence authority and frozen rollback
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          from pathlib import Path
          p=json.loads(Path('atlas-v2-release.json').read_text())
          l=json.loads(Path('atlas-release.json').read_text())
          runtime=json.loads(Path('atlas-runtime-manifest.json').read_text())
          build=json.loads(Path('build.json').read_text())
          assert p['release']=='2.0.2' and str(p['build'])=='2002'
          assert p['runtime']=='ATLAS_V2_ANALYTICS'
          assert runtime['release']==l['release']==build['app_version']
          assert str(runtime['build'])==str(l['build'])==str(build['build'])
          assert str(p['legacy_fallback']['release'])==str(l['release'])
          assert str(p['legacy_fallback']['build'])==str(l['build'])
          assert p['legacy_fallback']['authority_active_on_primary'] is False
          for path in [
            'atlas-v2.html','src/v2/atlas-v2-boot.js','src/v2/atlas-v2-viz.js','src/v2/atlas-v2-viz.css',
            'src/v2/entity-search-adapter.js','src/v2/explore-surface.js','src/v2/entity360-adapter.js',
            'src/v2/entity360-surface.js','src/v2/universes-adapter.js','tools/build_atlas_v2_primary.py'
          ]: assert Path(path).is_file(), path
          html=Path('atlas-v2.html').read_text()
          assert 'data-atlas-v2-primary="analytics"' in html
          assert './v2/atlas-v2-shell.css?v=v2-primary-5' in html
          assert './v2/atlas-v2-viz.css?v=v2-primary-5-viz1' in html
          assert './v2/atlas-v2-boot.js?v=v2-primary-5-entity-intelligence-1' in html
          boot=Path('src/v2/atlas-v2-boot.js').read_text()
          assert "STRUCTURAL_VERSION = 'v2-primary-5'" in boot
          assert "SURFACE_VERSION = 'v2-primary-5-entity-intelligence-1'" in boot
          explore=Path('src/v2/explore-surface.js').read_text()
          entity=Path('src/v2/entity360-surface.js').read_text()
          entity_adapter=Path('src/v2/entity360-adapter.js').read_text()
          universe=Path('src/v2/universes-adapter.js').read_text()
          assert 'AtlasV2Universes.attention' in explore and 'SO UAF ↔ SII' in explore
          assert all(x in entity for x in ['screeningPanel','reportingPanel','reconciliationPanel','digitalIdentityPanel'])
          assert all(x in entity_adapter for x in ['entity_intelligence','entity_screening_live','digital_identity_live','searchDigitalIdentity'])
          assert "query('attention'" in universe
          print('ATLAS 2.0.2 entity-intelligence source authority: OK')
          PY
          for f in \
            src/v2/atlas-v2-production-config.js src/v2/atlas-v2-health.js src/v2/atlas-v2-core-auth.js \
            src/v2/atlas-v2-data.js src/v2/atlas-v2-session.js src/v2/atlas-v2-access.js src/v2/atlas-v2-shell.js \
            src/v2/atlas-v2-boot.js src/v2/atlas-v2-viz.js src/v2/entity-search-adapter.js src/v2/explore-surface.js \
            src/v2/entity360-adapter.js src/v2/entity360-surface.js src/v2/public-spend-surface.js \
            src/v2/relations-surface.js src/v2/universes-adapter.js src/v2/universes-surface.js \
            src/v2/territory-adapter.js src/v2/territory-surface.js src/v2/watch-adapter.js src/v2/watch-surface.js; do node --check "$f"; done
          python3 -m py_compile tools/build_atlas_v2_primary.py

      - name: Build ATLAS v2 primary with explicit legacy rollback
        run: |
          set -euo pipefail
          python3 tools/build_atlas_v2_primary.py --out _site
          python3 - <<'PY'
          import json
          from pathlib import Path
          root=Path('_site')
          p=json.loads((root/'atlas-v2-release.json').read_text())
          l=json.loads((root/'atlas-release.json').read_text())
          r=json.loads((root/'atlas-runtime-report.json').read_text())
          html=(root/'index.html').read_text()
          legacy=(root/'legacy.html').read_text()
          assert r['release']=='2.0.2' and r['build']=='2002'
          assert r['v2_version']=='v2-primary-5'
          assert r['v2_explore_mode']=='LIVE_PULSE_VISUAL_UAF_SII'
          assert r['v2_entity_search']=='IDENTITY_TIERED_DIGITAL_PRESS'
          assert r['v2_entity_intelligence']=='UAF_SII_REPORTING_SCREENING_DIGITAL'
          assert r['legacy_fallback_policy']=='FROZEN_ROLLBACK_ONLY'
          assert r['legacy_authority_active_on_primary'] is False
          assert f'data-aml-version="{p["release"]}"' in html and f'data-aml-build="{p["build"]}"' in html
          assert './v2/atlas-v2-boot.js?v=v2-primary-5-entity-intelligence-1' in html
          assert './v2/atlas-v2-viz.css?v=v2-primary-5-viz1' in html
          assert 'atlas-runtime-current-' not in html
          assert f'data-aml-version="{l["release"]}"' in legacy and 'atlas-runtime-current-' in legacy
          for path in ['v2/atlas-v2-viz.js','v2/atlas-v2-viz.css','v2/entity-search-adapter.js','v2/explore-surface.js','v2/entity360-adapter.js','v2/entity360-surface.js','v2/universes-adapter.js']:
              assert (root/path).is_file(), path
          print('ATLAS 2.0.2 entity-intelligence artifact + frozen rollback: OK')
          PY

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: ./_site
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Verify live ATLAS 2.0.2 entity intelligence and rollback
        shell: bash
        env:
          PAGE_URL: ${{ steps.deployment.outputs.page_url }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json, os, time, urllib.request
          from pathlib import Path
          p=json.loads(Path('atlas-v2-release.json').read_text())
          l=json.loads(Path('atlas-release.json').read_text())
          rel=str(p['release']); bid=str(p['build'])
          lrel=str(l['release']); lbid=str(l['build'])
          base=os.environ['PAGE_URL'].rstrip('/')
          sha=os.environ.get('GITHUB_SHA','unknown')
          asset='v2-primary-5'
          surface='v2-primary-5-entity-intelligence-1'
          viz_style='v2-primary-5-viz1'
          def get(path, stamp):
              sep='&' if '?' in path else '?'
              req=urllib.request.Request(f'{base}{path}{sep}atlas={stamp}',headers={'Cache-Control':'no-cache','User-Agent':'ATLAS-2.0.2-entity-intelligence-smoke'})
              with urllib.request.urlopen(req,timeout=20) as r: return r.read().decode('utf-8','replace')
          last=None
          for n in range(1,13):
              try:
                  stamp=f'{sha}-{n}'
                  html=get('/',stamp)
                  live=json.loads(get('/atlas-v2-release.json',stamp))
                  report=json.loads(get('/atlas-runtime-report.json',stamp))
                  legacy=get('/legacy.html',stamp)
                  boot=get(f'/v2/atlas-v2-boot.js?v={surface}',stamp)
                  explore=get(f'/v2/explore-surface.js?v={surface}',stamp)
                  entity=get(f'/v2/entity360-surface.js?v={surface}',stamp)
                  entity_adapter=get(f'/v2/entity360-adapter.js?v={surface}',stamp)
                  universe=get(f'/v2/universes-adapter.js?v={surface}',stamp)
                  search=get(f'/v2/entity-search-adapter.js?v={surface}',stamp)
                  viz=get(f'/v2/atlas-v2-viz.js?v={surface}',stamp)
                  viz_css=get(f'/v2/atlas-v2-viz.css?v={viz_style}',stamp)
                  assert live['release']==rel and str(live['build'])==bid
                  assert f'data-aml-version="{rel}"' in html and f'data-aml-build="{bid}"' in html
                  assert f'./v2/atlas-v2-boot.js?v={surface}' in html and f'./v2/atlas-v2-viz.css?v={viz_style}' in html
                  assert 'atlas-runtime-current-' not in html
                  assert report['release']==rel and report['build']==bid and report['v2_version']==asset
                  assert report['v2_explore_mode']=='LIVE_PULSE_VISUAL_UAF_SII'
                  assert report['v2_entity_search']=='IDENTITY_TIERED_DIGITAL_PRESS'
                  assert report['v2_entity_intelligence']=='UAF_SII_REPORTING_SCREENING_DIGITAL'
                  assert f'data-aml-version="{lrel}"' in legacy and f'data-aml-build="{lbid}"' in legacy and 'atlas-runtime-current-' in legacy
                  assert "STRUCTURAL_VERSION = 'v2-primary-5'" in boot and "SURFACE_VERSION = 'v2-primary-5-entity-intelligence-1'" in boot
                  assert 'AtlasV2Universes.attention' in explore and 'SO UAF ↔ SII' in explore
                  assert all(x in entity for x in ['screeningPanel','reportingPanel','reconciliationPanel','digitalIdentityPanel'])
                  assert all(x in entity_adapter for x in ['entity_intelligence','entity_screening_live','digital_identity_live','searchDigitalIdentity'])
                  assert "query('attention'" in universe and 'recentTerminated' in universe
                  assert 'ATLAS_ENTITY_SEARCH_V2' in search and "operation: 'entity_search'" in search and 'resultTier' in search
                  assert all(x in viz for x in ['horizontalBars','lineChart','segmented'])
                  assert all(x in viz_css for x in ['.atlas-v2-viz-bar-fill','.atlas-v2-viz-segment-track','.atlas-v2-viz-legend-item'])
                  print(json.dumps({'status':'ok','release':rel,'build':bid,'asset':asset,'surface':surface,'viz_style':viz_style,'entity_intelligence':'UAF_SII_REPORTING_SCREENING_DIGITAL','legacy_release':lrel,'legacy_build':lbid}))
                  break
              except Exception as exc:
                  last=exc
                  print(f'live smoke attempt {n}/12 not ready: {exc}')
                  if n<12: time.sleep(8)
          else:
              raise SystemExit(f'ATLAS 2.0.2 entity-intelligence live smoke failed: {last}')
          PY
