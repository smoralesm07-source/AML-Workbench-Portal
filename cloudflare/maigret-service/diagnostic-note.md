# Cloudflare Containers preflight

Temporary deployment diagnostic used to distinguish Containers entitlement/plan failures (HTTP 403) from token/backend authorization failures (HTTP 401) before rebuilding the Maigret image.
