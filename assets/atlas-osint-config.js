'use strict';

/* Configuración pública del conector OSINT.
 * No contiene secretos. `apiBase` debe apuntar al microservicio
 * services/atlas-osint-sherlock desplegado detrás de HTTPS.
 */
window.__ATLAS_OSINT_CONFIG__={
  enabled:true,
  apiBase:''
};
