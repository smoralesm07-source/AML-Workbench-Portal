(function(){
'use strict';
const VIEW='pep-discovery',TABLE='aml_pep_discovery_snapshot';
const cache={payload:null,loadedAt:0,error:null,promise:null,transport:null};
const ui={query:'',review:'ALL',linkage:'ALL',signal:'ALL'};
let hookedShell=null,hookedNavigate=null;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=v=>Number.isFinite(Number(v))?Number(v):0;
const fmt=(v,d=0)=>num(v).toLocaleString('es-CL',{minimumFractionDigits:d,maximumFractionDigits:d});
const clp=v=>{const n=num(v);if(!n)return'$0';if(Math.abs(n)>=1e9)return'$'+(n/1e9).toLocaleString('es-CL',{maximumFractionDigits:1})+' mil MM';if(Math.abs(n)>=1e6)return'$'+(n/1e6).toLocaleString('es-CL',{maximumFractionDigits:1})+' MM';return'$'+Math.round(n).toLocaleString('es-CL');};
const pct=v=>Number.isFinite(Number(v))?Number(v).toLocaleString('es-CL',{maximumFractionDigits:1})+'%':'—';
const arr=v=>Array.isArray(v)?v:[],obj=v=>v&&typeof v==='object'&&!Array.isArray(v)?v:{};
const isActive=()=>window.state?.view===VIEW;
function client(){try{return typeof sb!=='undefined'?sb:(window.sb||null);}catch(_e){return window.sb||null;}}
function icon(){return'<span class="atlas-nav-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><circle cx="7" cy="7" r="2.4"/><circle cx="17" cy="7" r="2.4"/><circle cx="12" cy="17" r="2.4"/><path d="M9.2 7h5.6M8.4 9l2.4 5.7M15.6 9l-2.4 5.7"/></svg></span>';}
function ensureNav(){const nav=document.querySelector('.v019-nav');if(!nav)return false;let b=nav.querySelector('[data-atlas-pep-view="1"]');if(!b){b=document.createElement('button');b.type='button';b.className='v019-nav-btn atlas-nav-btn atlas-pep-nav-button';b.dataset.atlasPepView='1';b.setAttribute('aria-label','Personas y control');b.innerHTML=icon()+'<span class="atlas-nav-text">Personas y control</span><span class="atlas-nav-chevron" aria-hidden="true">›</span>';b.addEventListener('click',()=>window.navigate?.(VIEW));}const q=nav.querySelector('.v019-nav-btn[data-view="questions"]');if(q&&q.nextElementSibling!==b)q.insertAdjacentElement('afterend',b);else if(!b.isConnected)nav.appendChild(b);b.classList.toggle('active',isActive());b.setAttribute('aria-current',isActive()?'page':'false');return true;}
function setNavActive(){document.querySelectorAll('.v019-nav-btn').forEach(b=>b.classList.toggle('active',b.dataset.atlasPepView==='1'?isActive():b.dataset.view===window.state?.view));ensureNav();}
function validate(p){if(!p||p.schema!=='ATLAS_PEP_DISCOVERY_LATEST_V1')throw new Error('Contrato PEP no reconocido');if(!Array.isArray(p.top_cases)||!p.coverage||!p.lineage)throw new Error('Contrato PEP incompleto');return p;}
async function loadFromDb(){const db=client();if(!db)throw new Error('Sesión de datos ATLAS no disponible');const {data,error}=await db.from(TABLE).select('payload,source_generated_at,coverage_status,ingested_at').eq('snapshot_key','latest').maybeSingle();if(error)throw error;if(!data?.payload)throw new Error('El snapshot PEP todavía no ha sido materializado');cache.transport='SUPABASE_RLS';return validate(data.payload);}
async function loadFromConfiguredEndpoint(){const url=window.ATLAS_PEP_DISCOVERY_SOURCE;if(!url)throw new Error('Sin endpoint alternativo configurado');const ctl=new AbortController(),timer=setTimeout(()=>ctl.abort(),8000);try{const r=await fetch(String(url),{cache:'no-store',credentials:'same-origin',signal:ctl.signal,headers:{Accept:'application/json'}});if(!r.ok)throw new Error('HTTP '+r.status);cache.transport='CONFIGURED_ENDPOINT';return validate(await r.json());}finally{clearTimeout(timer);}}
async function load(force=false){if(cache.payload&&!force)return cache.payload;if(cache.promise&&!force)return cache.promise;cache.promise=(async()=>{try{let p;try{p=await loadFromDb();}catch(dbError){if(!window.ATLAS_PEP_DISCOVERY_SOURCE)throw dbError;p=await loadFromConfiguredEndpoint();}cache.payload=p;cache.loadedAt=Date.now();cache.error=null;return p;}catch(e){cache.error=e;throw e;}finally{cache.promise=null;}})();return cache.promise;}
function coverage(p){return obj(p.coverage);}function casesCount(p,k){return num(obj(p.cases)[k]);}function bo(p,k){return num(obj(p.beneficial_ownership)[k]);}function pepCount(p){return num(obj(p.pep).confirmed_for_discovery);}function generated(p){if(!p.generated_at)return'corte no informado';try{return new Intl.DateTimeFormat('es-CL',{dateStyle:'medium',timeStyle:'short'}).format(new Date(p.generated_at));}catch{return String(p.generated_at);}}
function flow(p){return`<div class="atlas-pep-flow" aria-label="Cadena analítica"><div class="atlas-pep-flow-step"><small>1 · Resolver PEP</small><b>${fmt(pepCount(p))}</b><span>personas confirmadas para discovery</span></div><div class="atlas-pep-flow-step"><small>2 · Propiedad</small><b>${fmt(bo(p,'downstream_targets'))}</b><span>sociedades BF ≥ umbral aguas abajo</span></div><div class="atlas-pep-flow-step"><small>3 · Compras</small><b>${fmt(bo(p,'supplier_targets'))}</b><span>proveedores objetivo contrastados</span></div><div class="atlas-pep-flow-step"><small>4 · Lectura</small><b>${fmt(casesCount(p,'cases'))}</b><span>relaciones consolidadas</span></div></div>`;}
function kpis(p){const c=coverage(p);return`<div class="atlas-pep-kpis"><article class="atlas-pep-kpi"><span class="lab">PEP resueltas</span><b>${fmt(pepCount(p))}</b><small>Circular 62 gobernada</small></article><article class="atlas-pep-kpi"><span class="lab">BF downstream</span><b>${fmt(bo(p,'downstream_targets'))}</b><small>BF económico ≥10%</small></article><article class="atlas-pep-kpi"><span class="lab">Proveedor + BF PEP</span><b>${fmt(bo(p,'suppliers_with_pep_bf'))}</b><small>contexto, no señal adversa</small></article><article class="atlas-pep-kpi"><span class="lab">Casos contexto</span><b>${fmt(casesCount(p,'context'))}</b><small>relación explicable</small></article><article class="atlas-pep-kpi hot"><span class="lab">Para revisar</span><b>${fmt(casesCount(p,'review'))}</b><small>sólo PEP-03 / PEP-04</small></article><article class="atlas-pep-kpi"><span class="lab">Órdenes cargadas</span><b>${fmt(c.orders_loaded)}</b><small>${fmt(c.orders_matched)} coincidentes</small></article></div>`;}
function mix(p){const a=casesCount(p,'bf_direct'),b=casesCount(p,'bf_indirect'),c=casesCount(p,'control_only'),t=Math.max(1,a+b+c),d1=(a/t*100).toFixed(2)+'%',d2=((a+b)/t*100).toFixed(2)+'%';return`<div class="atlas-pep-donut-wrap"><div class="atlas-pep-donut" style="--d1:${d1};--d2:${d2}"><div class="atlas-pep-donut-center"><b>${fmt(a+b+c)}</b><span>vínculos</span></div></div><div class="atlas-pep-legend"><div class="atlas-pep-legend-row"><i style="--c:var(--pep-accent)"></i><span>BF directo calculado</span><b>${fmt(a)}</b></div><div class="atlas-pep-legend-row"><i style="--c:#7e77cf"></i><span>BF indirecto calculado</span><b>${fmt(b)}</b></div><div class="atlas-pep-legend-row"><i style="--c:var(--pep-muted)"></i><span>Control declarado</span><b>${fmt(c)}</b></div></div></div>`;}
function periods(p){const down=new Set(arr(p.downloaded_periods).map(String)),fail=new Set(arr(p.failed_periods).map(x=>`${x.year}-${String(x.month).padStart(2,'0')}`));return`<div class="atlas-pep-periods">${arr(p.requested_periods).map(v=>{const s=String(v).replace(/-(\d)$/,'-0$1'),cls=down.has(s)||down.has(String(v))?'ok':fail.has(s)?'fail':'';return`<span class="atlas-pep-period ${cls}">${esc(s)}</span>`;}).join('')}</div>`;}
function topBars(p){const rows=arr(p.top_cases).slice(0,7);if(!rows.length)return'<div class="atlas-pep-note">Sin casos en el corte actual.</div>';const m=Math.max(...rows.map(x=>num(x.amount_total_clp)),1);return`<div class="atlas-pep-bars">${rows.map(c=>`<div class="atlas-pep-bar"><span class="atlas-pep-bar-label" title="${esc(c.company_name||c.company_rut)}">${esc(c.company_name||c.company_rut||'Entidad')}</span><div class="atlas-pep-track"><div class="atlas-pep-fill" style="--w:${Math.max(2,num(c.amount_total_clp)/m*100).toFixed(1)}%"></div></div><span class="atlas-pep-bar-value">${esc(clp(c.amount_total_clp))}</span></div>`).join('')}</div>`;}
function linkageLabel(v){return({BF_CALCULATED_DIRECT:'BF directo',BF_CALCULATED_INDIRECT:'BF indirecto',CONTROL_DECLARED:'Control declarado'})[v]||String(v||'Vínculo');}
function filters(rows){const links=[...new Set(rows.map(x=>x.linkage_strength).filter(Boolean))],signals=[...new Set(rows.flatMap(x=>[...arr(x.relationship_signals),...arr(x.review_signals)]))].sort();return`<div class="atlas-pep-toolbar"><label class="atlas-pep-search"><svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"/><path d="m16.5 16.5 4 4"/></svg><input id="atlas-pep-query" class="atlas-pep-field" type="search" value="${esc(ui.query)}" placeholder="Persona, empresa, RUT o institución"></label><select id="atlas-pep-review" class="atlas-pep-field"><option value="ALL">Todos los niveles</option><option value="REVIEW" ${ui.review==='REVIEW'?'selected':''}>Para revisar</option><option value="CONTEXT" ${ui.review==='CONTEXT'?'selected':''}>Contexto</option></select><select id="atlas-pep-linkage" class="atlas-pep-field"><option value="ALL">Todos los vínculos</option>${links.map(v=>`<option value="${esc(v)}" ${ui.linkage===v?'selected':''}>${esc(linkageLabel(v))}</option>`).join('')}</select><select id="atlas-pep-signal" class="atlas-pep-field"><option value="ALL">Todas las señales</option>${signals.map(v=>`<option value="${esc(v)}" ${ui.signal===v?'selected':''}>${esc(v)}</option>`).join('')}</select></div>`;}
function filtered(p){const q=ui.query.trim().toLowerCase();return arr(p.top_cases).filter(c=>{if(ui.review!=='ALL'&&c.review_level!==ui.review)return false;if(ui.linkage!=='ALL'&&c.linkage_strength!==ui.linkage)return false;const sig=[...arr(c.relationship_signals),...arr(c.review_signals)];if(ui.signal!=='ALL'&&!sig.includes(ui.signal))return false;return!q||[c.person_name,c.company_name,c.company_rut,c.position,c.institution,...sig].some(v=>String(v||'').toLowerCase().includes(q));});}
function caseRows(p){const rows=filtered(p);if(!rows.length)return'<div class="atlas-pep-empty"><div class="ico">⌕</div><h3>Sin coincidencias para estos filtros</h3><p>La ausencia en esta vista no equivale a ausencia de contratación pública fuera de los períodos cargados.</p></div>';return`<div class="atlas-pep-results">${rows.map(c=>{const rel=arr(c.relationship_signals),rev=arr(c.review_signals);return`<article class="atlas-pep-case" tabindex="0" role="button" data-case-key="${esc(c.case_key||'')}"><div class="atlas-pep-case-main"><div class="atlas-pep-case-top"><strong>${esc(c.company_name||c.company_rut||'Entidad')}</strong><span class="atlas-pep-pill ${c.review_level==='REVIEW'?'review':''}">${c.review_level==='REVIEW'?'Revisar':'Contexto'}</span></div><p>${esc(c.person_name||'Persona')} · ${esc(c.position||'cargo no informado')} · ${esc(c.company_rut||'')}</p><div class="atlas-pep-tags"><span class="atlas-pep-tag rel">${esc(linkageLabel(c.linkage_strength))}${c.beneficial_owner_pct!=null?' · '+esc(pct(c.beneficial_owner_pct)):''}</span>${rel.map(s=>`<span class="atlas-pep-tag rel">${esc(s)}</span>`).join('')}${rev.map(s=>`<span class="atlas-pep-tag rev">${esc(s)}</span>`).join('')}</div></div><div class="atlas-pep-metric"><span>Compras</span><b>${fmt(c.order_count)} OC · ${esc(clp(c.amount_total_clp))}</b></div><div class="atlas-pep-metric"><span>Concentración</span><b>HHI ${fmt(c.buyer_amount_hhi,2)} · top ${pct(num(c.top_buyer_amount_share)*100)}</b></div><div class="atlas-pep-chevron">›</div></article>`;}).join('')}</div>`;}
function render(p){const h=document.querySelector('#content');if(!h)return;const failed=arr(p.failed_periods).length;h.innerHTML=`<section class="atlas-pep" data-atlas-pep-root="1"><div class="atlas-pep-hero"><div class="atlas-pep-hero-row"><div><div class="atlas-pep-kicker">Análisis · Personas, propiedad y Estado</div><h2>Descubrimiento PEP y beneficiario final</h2><p>Relaciona cargos PEP resueltos, propiedad económica, control declarado y compras públicas. Prioriza revisión humana sin convertir la condición PEP en score AML.</p></div><div class="atlas-pep-status"><span class="atlas-pep-pill ${String(p.status).toUpperCase()==='COMPLETE'?'complete':'partial'}">${String(p.status).toUpperCase()==='COMPLETE'?'Cobertura completa':'Cobertura parcial'}</span>${failed?`<span class="atlas-pep-pill review">${fmt(failed)} período${failed===1?'':'s'} con incidencia</span>`:''}<span class="atlas-pep-pill">${esc(generated(p))}</span></div></div>${flow(p)}</div>${kpis(p)}<div class="atlas-pep-grid"><article class="atlas-pep-panel"><div class="atlas-pep-panel-head"><div><h3>Estructura de los vínculos visibles</h3><p>BF económico directo/indirecto y control declarado se muestran separados.</p></div></div>${mix(p)}</article><article class="atlas-pep-panel"><div class="atlas-pep-panel-head"><div><h3>Cobertura temporal ChileCompra</h3><p>Un período no cargado nunca se interpreta como cero contratación.</p></div></div>${periods(p)}</article></div><article class="atlas-pep-panel"><div class="atlas-pep-panel-head"><div><h3>Materialidad de los casos visibles</h3><p>Monto de órdenes comprometidas; no representa pago efectivo.</p></div></div>${topBars(p)}</article>${filters(arr(p.top_cases))}<div id="atlas-pep-case-results">${caseRows(p)}</div><div class="atlas-pep-note"><strong>Regla de lectura.</strong> PEP-01, PEP-02 y PEP-05 describen relación o contexto. Sólo PEP-03/PEP-04 llevan un caso a <em>Revisar</em>. El orden es triage analítico, no probabilidad de delito ni score LA/FT.</div></section>`;bind(p);setNavActive();}
function renderLoading(){const h=document.querySelector('#content');if(h)h.innerHTML='<div class="atlas-pep atlas-pep-loading">Cargando relaciones bajo RLS…</div>';}
function renderUnavailable(e){const h=document.querySelector('#content');if(!h)return;h.innerHTML=`<section class="atlas-pep"><div class="atlas-pep-empty"><div class="ico">◇</div><h3>El snapshot de descubrimiento aún no está disponible</h3><p>ATLAS sigue operativo. La vista se activará cuando el pipeline materialice el primer corte gobernado en el canal privado. No se muestran datos de ejemplo.</p><button id="atlas-pep-retry" type="button">Reintentar</button></div><div class="atlas-pep-note"><strong>Canal:</strong> Supabase/RLS · ${esc(e?.message||String(e||'sin snapshot'))}</div></section>`;document.querySelector('#atlas-pep-retry')?.addEventListener('click',()=>open(true));setNavActive();}
function bind(p){const refresh=()=>{const h=document.querySelector('#atlas-pep-case-results');if(h)h.innerHTML=caseRows(p);bindCases(p);};document.querySelector('#atlas-pep-query')?.addEventListener('input',e=>{ui.query=e.target.value;clearTimeout(e.target._pepTimer);e.target._pepTimer=setTimeout(refresh,120);});for(const [id,key] of [['atlas-pep-review','review'],['atlas-pep-linkage','linkage'],['atlas-pep-signal','signal']])document.querySelector('#'+id)?.addEventListener('change',e=>{ui[key]=e.target.value;refresh();});bindCases(p);}
function bindCases(p){document.querySelectorAll('[data-case-key]').forEach(card=>{const go=()=>{const c=arr(p.top_cases).find(x=>String(x.case_key||'')===card.dataset.caseKey);if(c)openDrawer(c);};card.addEventListener('click',go);card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();go();}});});}
function pathHtml(paths){const nodes=arr(arr(paths)[0]?.node_path);if(!nodes.length)return'<div class="atlas-pep-method">Sin camino económico serializado en este corte.</div>';return`<div class="atlas-pep-path">${nodes.map((n,i)=>`${i?'<span class="atlas-pep-arrow">→</span>':''}<span class="atlas-pep-node" title="${esc(n)}">${esc(n)}</span>`).join('')}</div>`;}
function openDrawer(c){const rel=arr(c.relationship_signals),rev=arr(c.review_signals);const html=`<div class="atlas-pep atlas-pep-drawer"><div><div class="atlas-pep-kicker">Relación trazable</div><h2>${esc(c.company_name||c.company_rut||'Entidad')}</h2><div class="atlas-pep-drawer-sub">${esc(c.company_rut||'')} · ${esc(c.person_name||'Persona')} · ${esc(c.position||'cargo no informado')} · ${esc(c.institution||'institución no informada')}</div></div><div class="atlas-pep-tags"><span class="atlas-pep-tag rel">${esc(linkageLabel(c.linkage_strength))}</span>${rel.map(s=>`<span class="atlas-pep-tag rel">${esc(s)}</span>`).join('')}${rev.map(s=>`<span class="atlas-pep-tag rev">${esc(s)}</span>`).join('')}</div><div class="atlas-pep-drawer-kpis"><div class="atlas-pep-drawer-kpi"><span>Beneficio económico</span><b>${c.beneficial_owner_pct==null?'No calculado':esc(pct(c.beneficial_owner_pct))}</b></div><div class="atlas-pep-drawer-kpi"><span>Nivel de lectura</span><b>${c.review_level==='REVIEW'?'Revisar':'Contexto'}</b></div><div class="atlas-pep-drawer-kpi"><span>Órdenes</span><b>${fmt(c.order_count)} · ${esc(clp(c.amount_total_clp))}</b></div><div class="atlas-pep-drawer-kpi"><span>Compradores</span><b>${fmt(c.distinct_buyers)} · HHI ${fmt(c.buyer_amount_hhi,2)}</b></div></div><div><div class="atlas-pep-kicker">Camino de propiedad</div>${pathHtml(c.beneficial_owner_paths)}</div><div class="atlas-pep-method"><strong>Cómo leer este caso.</strong><br>${esc(c.guardrail||'La relación PEP/BF es contexto de debida diligencia y no una conclusión de irregularidad.')}<br><br><strong>Prioridad de compras:</strong> ${fmt(c.procurement_priority_score,1)} / 100 — triage, no score AML.</div></div>`;if(typeof window.v019OpenDrawer==='function')window.v019OpenDrawer(html);else{const body=document.querySelector('#v019-drawer-body'),drawer=document.querySelector('#v019-drawer');if(body&&drawer){body.innerHTML=html;drawer.classList.add('open');}}}
async function open(force=false){if(window.state)window.state.view=VIEW;try{if(typeof window.shell==='function')window.shell('Personas y control','PEP, beneficiario final y compras públicas con trazabilidad y guardrails explícitos.');}catch(_e){}setNavActive();renderLoading();try{const p=await load(force);render(p);try{window.audit?.('VIEW_PEP_DISCOVERY',{objectType:'analytical_view',objectId:VIEW,payload:{cases:casesCount(p,'cases'),review:casesCount(p,'review'),coverage:String(p.status||''),transport:cache.transport}});}catch(_e){}}catch(e){renderUnavailable(e);}}
function installHooks(){if(typeof window.shell==='function'&&window.shell!==hookedShell&&!window.shell.__atlasPepWrapped){const base=window.shell,wrapped=function(...a){const r=base.apply(this,a);queueMicrotask(ensureNav);return r;};wrapped.__atlasPepWrapped=true;window.shell=wrapped;hookedShell=wrapped;}if(typeof window.navigate==='function'&&window.navigate!==hookedNavigate&&!window.navigate.__atlasPepWrapped){const base=window.navigate,wrapped=async function(view,...rest){if(view===VIEW)return open(false);const r=await base.call(this,view,...rest);queueMicrotask(()=>{ensureNav();setNavActive();});return r;};wrapped.__atlasPepWrapped=true;window.navigate=wrapped;hookedNavigate=wrapped;}ensureNav();}
function boot(){installHooks();window.addEventListener('atlas:nav-refresh',()=>queueMicrotask(ensureNav));window.addEventListener('atlas:themechange',()=>{if(isActive()&&cache.payload)render(cache.payload);});}
window.AtlasPepDiscovery={view:VIEW,table:TABLE,load,open,refresh:()=>open(true),ensureNav,health:()=>({status:cache.payload?'ready':cache.error?'degraded':'idle',transport:cache.transport,loadedAt:cache.loadedAt||null,error:cache.error?String(cache.error.message||cache.error):null,storagePolicy:'MEMORY_ONLY',accessPolicy:'SUPABASE_RLS_AUTHENTICATED_ENABLED_USERS'})};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
