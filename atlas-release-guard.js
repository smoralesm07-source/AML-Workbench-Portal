'use strict';
/* ATLAS AML · single active release authority 0.70.5 */
(function atlasSingleReleaseAuthority(){
  const PRODUCT='ATLAS AML',TAGLINE='Plataforma Integrada de Inteligencia y Riesgo',MANIFEST='./atlas-release.json';
  const root=document.documentElement;
  const active={release:'0.70.5',build:'0705'};
  let applying=false,observer=null,queued=false;
  function setAttrIfChanged(name,value){if(root.getAttribute(name)!==value)root.setAttribute(name,value);}
  function replaceLegacyVersionText(){
    document.querySelectorAll('.topbar .eyebrow,.v18-pagehead .eyebrow').forEach(el=>{const wanted=`${PRODUCT} · v${active.release}`;if(el.textContent!==wanted)el.textContent=wanted;});
    document.querySelectorAll('.v019-brand').forEach(brand=>{const strong=brand.querySelector('strong');if(strong&&strong.textContent!==PRODUCT)strong.textContent=PRODUCT;const small=brand.querySelector('small');if(small){const label=`v${active.release}`;small.textContent=label;small.dataset.activeVersion=active.release;small.setAttribute('aria-label',`Versión ${active.release}`);small.setAttribute('data-runtime-label',label);}});
    document.querySelectorAll('[data-atlas-version-label],.atlas-version,.version-badge,.app-version').forEach(el=>{if(/51\.1|0\.51\.1/i.test(el.textContent||''))el.textContent=`v${active.release}`;});
  }
  function applyRelease(){if(applying)return;applying=true;try{setAttrIfChanged('data-aml-version',active.release);setAttrIfChanged('data-aml-build',active.build);setAttrIfChanged('data-atlas-release',active.release);setAttrIfChanged('data-atlas-release-policy','single-active');window.__AML_ACTIVE_VERSION__=active.release;window.__AML_BUILD__=active.build;window.__ATLAS_ACTIVE_VERSION__=active.release;window.__ATLAS_RELEASE_BUILD__=active.build;window.__AML_VERSION_SOURCE__='atlas-release-guard-0705';const wantedTitle=`${PRODUCT} · v${active.release}`;if(document.title!==wantedTitle)document.title=wantedTitle;const meta=document.querySelector('meta[name="application-name"]');if(meta&&meta.content!==`${PRODUCT} · ${TAGLINE}`)meta.content=`${PRODUCT} · ${TAGLINE}`;replaceLegacyVersionText();window.__ATLAS_RELEASE_GUARD_HEALTH__={status:'ready',release:active.release,build:active.build,visibleVersionPolicy:'ATLAS_RELEASE_GUARD_0705_ONLY',runtimePolicy:'CANONICAL_COMPILED_RUNTIME_ONLY',updatePolicy:'MANIFEST_PROMOTED_RELEASE_ACTIVE',checkedAt:new Date().toISOString()};}finally{applying=false;}}
  function queueApply(){if(queued)return;queued=true;queueMicrotask(()=>{queued=false;applyRelease();});}
  function watchRelease(){applyRelease();if(observer)return;observer=new MutationObserver(()=>{if(root.getAttribute('data-aml-version')!==active.release||root.getAttribute('data-aml-build')!==active.build||root.getAttribute('data-atlas-release')!==active.release)queueApply();replaceLegacyVersionText();});observer.observe(root,{attributes:true,attributeFilter:['data-aml-version','data-aml-build','data-atlas-release']});new MutationObserver(queueApply).observe(document.documentElement,{childList:true,subtree:true});}
  async function verifyManifest(){try{const url=new URL(MANIFEST,location.href);url.searchParams.set('_atlas',Date.now().toString(36));const res=await fetch(url.toString(),{cache:'no-store',headers:{'Cache-Control':'no-cache'}});if(!res.ok)throw new Error(`release manifest HTTP ${res.status}`);const manifest=await res.json();window.__ATLAS_RELEASE_MANIFEST__=manifest;const mismatch=String(manifest.release)!==active.release||String(manifest.build)!==active.build;window.__ATLAS_RELEASE_MISMATCH__=mismatch?{active:{...active},manifest:{release:String(manifest.release),build:String(manifest.build)},action:'manifest-needs-alignment',checkedAt:new Date().toISOString()}:null;applyRelease();return !mismatch;}catch(error){window.__ATLAS_RELEASE_GUARD_HEALTH__={status:'manifest-unavailable',release:active.release,build:active.build,error:String(error?.message||error),checkedAt:new Date().toISOString()};applyRelease();return false;}}
  const api={product:PRODUCT,policy:'SINGLE_ACTIVE_RELEASE',runtimePolicy:'CANONICAL_COMPILED_RUNTIME_ONLY',apply:applyRelease,verify:verifyManifest};Object.defineProperties(api,{version:{get:()=>active.release},build:{get:()=>active.build}});window.AtlasRelease=api;applyRelease();if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',watchRelease,{once:true});else watchRelease();window.addEventListener('pageshow',()=>void verifyManifest());window.addEventListener('focus',()=>void verifyManifest());window.addEventListener('atlas:themechange',queueApply);void verifyManifest();for(const ms of [0,120,350,900,1800,4000])setTimeout(applyRelease,ms);
})();
